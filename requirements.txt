# FinLens — Python dependencies
# Install: pip install -r requirements.txt
#
# Core (required for tests + Streamlit app in demo mode)
numpy>=1.26
pandas>=2.1
scipy>=1.12
scikit-learn>=1.4
statsmodels>=0.14
plotly>=5.20
streamlit>=1.33
pytest>=8.0
pytest-cov>=5.0

# BigQuery connector (live mode)
google-cloud-bigquery>=3.19
google-cloud-bigquery-storage>=2.25
db-dtypes>=1.2

# dbt
dbt-core>=1.8
dbt-bigquery>=1.8

# Airflow (optional — for orchestration DAGs)
# apache-airflow==2.9.1
# apache-airflow-providers-google>=10.14

# Optional — Scenario 5: CausalForest CATE (EconML)
# econml>=0.15

# Optional — Scenario 2: full Callaway & Sant'Anna
# pyfixest>=0.18

# Optional — synthetic control (future scenario)
# pysyncon>=0.4

# Misc utilities
python-dotenv>=1.0
requests>=2.31
